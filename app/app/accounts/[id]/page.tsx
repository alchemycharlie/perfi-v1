export default function AccountDetailPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-text-primary">Account Detail</h1>
      <p className="mt-2 text-sm text-text-muted">
        Account transactions and balance history — coming in Phase C
      </p>
    </div>
  );
}
